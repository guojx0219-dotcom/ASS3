diff --git a/SPD-classes/src/main/java/com/watabou/utils/FileUtils.java b/SPD-classes/src/main/java/com/watabou/utils/FileUtils.java
index c259e22..8b4dd9e 100644
--- a/SPD-classes/src/main/java/com/watabou/utils/FileUtils.java
+++ b/SPD-classes/src/main/java/com/watabou/utils/FileUtils.java
@@ -138,0 +139,15 @@ public class FileUtils {
+	// Simple helper used by save recovery.
+	public static boolean copyDir( String from, String to ){
+		FileHandle oldDir = getFileHandle( from );
+		FileHandle newDir = getFileHandle( to );
+
+		if (oldDir == null || !oldDir.exists() || !oldDir.isDirectory()){
+			return false;
+		}
+		if (newDir.exists()){
+			newDir.deleteDirectory();
+		}
+		oldDir.copyTo( newDir );
+		return newDir.exists() && newDir.isDirectory();
+	}
+
@@ -163,0 +179,11 @@ public class FileUtils {
+	// Make folder if it does not exist.
+	public static boolean makeDir( String name ){
+		FileHandle dir = getFileHandle( name );
+
+		if (dir == null){
+			return false;
+		}
+		dir.mkdirs();
+		return dir.exists() && dir.isDirectory();
+	}
+
diff --git a/core/src/main/java/com/shatteredpixel/shatteredpixeldungeon/Dungeon.java b/core/src/main/java/com/shatteredpixel/shatteredpixeldungeon/Dungeon.java
index 08a63be..7110649 100644
--- a/core/src/main/java/com/shatteredpixel/shatteredpixeldungeon/Dungeon.java
+++ b/core/src/main/java/com/shatteredpixel/shatteredpixeldungeon/Dungeon.java
@@ -836,0 +837,2 @@ public class Dungeon {
+		SaveRecovery.backupBeforeDelete( save );
+
